<%= form_for(review_harvest) do |f| %>
  <% if review_harvest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_harvest.errors.count, "error") %> prohibited this review_harvest from being saved:</h2>

      <ul>
      <% review_harvest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :error_ids %>
    <%= f.text_area :error_ids %>
  </div>

  <div class="field">
    <%= f.label :updated_ids %>
    <%= f.text_area :updated_ids %>
  </div>

  <div class="field">
    <%= f.label :created_ids %>
    <%= f.text_area :created_ids %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
