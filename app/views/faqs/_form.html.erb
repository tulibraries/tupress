<%= form_for(faq) do |f| %>
  <% if faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faq.errors.count, "error") %> prohibited this faq from being saved:</h2>

      <ul>
      <% faq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :q %>
    <%= f.text_field :q, size: 100 %>
  </div>

  <div class="field">
    <%= f.label :a %>
    <%= f.text_area :a, cols: 100, rows: 20 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
