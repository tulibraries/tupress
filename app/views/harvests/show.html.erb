<p id="notice"><%= notice %></p>
<h1>Updated Titles</h1>
<ol>
  <% @created_books.each do |book| %>
    <li><%= link_to  book.title, "/book/"+book.book_id %></li>
  <% end %>
  <% @updated_books.each do |book| %>
    <li><%= link_to  book.title, "/book/"+book.book_id %></li>
  <% end %>
</ol>


<%= link_to 'Back', harvests_path %>
